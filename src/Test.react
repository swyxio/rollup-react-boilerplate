let $count = 0

export const STYLE = `
  button {
color: ${$count > 5 ? 'red' : 'blue'}
  }
`


export default () => {
  return <button onClick={() => $count++}>
  styled-jsx works! {$count}
</button>
}


// export default () => {
//   return <div>
//     Some Text
//   </div>
// }
